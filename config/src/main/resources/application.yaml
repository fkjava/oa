
spring:
   # Data Source
   datasource:
      driverClassName: com.mysql.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/oa?useSSL=false
      username: root
      password: 1234

   # JPA Config
   jpa:
      database-platform: MySQL57
      hibernate:
         ddl-auto: update

   # Spring Data Config
   #data:
   #   repositories:
   #      enabled: true

   # Web MVC ViewResolver Config
   mvc:
      view:
         prefix: /WEB-INF/views/
         suffix: .jsp
